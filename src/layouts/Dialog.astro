<div id="dialog">
    <div class="bar instructions fade-step">
        Use the ↑ and ↓ keys to navigate. Use &lt;TAB&gt; to switch between
        contexts. Press &lt;ESC&gt; at any time to return to the main menu.
    </div>
    <slot class="fade-step" />
    <div class="bar"></div>
</div>

<style>
    body {
        background-color: #6a9fb5;
    }

    #dialog {
        width: 100%;
        height: 100vh;
    }

    .bar {
        position: fixed;
        width: 100%;
        height: 25px;
        background-color: #c2c2c2;
    }

    .bar:nth-child(1) {
        padding-left: 5px;
    }

    .bar:nth-child(2) {
        bottom: 0;
        visibility: hidden;
    }

    /* Appearing on screen */
    .fade-step {
        opacity: 0;
        animation: fadeIn 0s linear forwards;
    }

    .fade-step:nth-child(1) {
        animation-delay: 100ms;
    }
    .fade-step:nth-child(2) {
        animation-delay: 150ms;
    }

    @keyframes fadeIn {
        from {
            opacity: 0;
        }
        to {
            opacity: 1;
        }
    }
</style>
